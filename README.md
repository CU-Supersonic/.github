##**Purpose:** 
This is a special repository that **doesn't hold project code**. It holds our `CU-Supersonic` landing page (called `profile/README.md`) as well as any files or templates meant to be accessed and applied to every repo in the org.
